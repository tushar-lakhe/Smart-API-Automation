name: run

on: 
    workflow_dispatch:

jobs:
 build:
    runs-on: windows-latest
    steps:
        
        - name: checkout repo content
          uses: actions/checkout@v3
          with:
            repository: tushar-lakhe/Smart-API-Automation
          
        - name: setup python
          uses: actions/setup-python@v4
          with: 
            python-version: '3.9'
        
        - name: List dir
          run: |
                pwd
                ls
          
        - name: execute py script
          run: python SmartAPIautomationframework>python Smartapiautomationframework\src\test\java\examples\users\pythonController\DataValidators.py
